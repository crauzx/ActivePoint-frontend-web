export class Users {
    email:string
    role:string
    point:number

    constructor(email:string, role:string, point:number){
        this.email = email
        this.role = role
        this.point = point
    }
}
